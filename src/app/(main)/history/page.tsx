export default function HistoryPage() {
  return (
    <section className="mx-auto w-full max-w-5xl px-6 py-12">
      <h1 className="font-display text-4xl text-text-primary">
        Download history
      </h1>
      <p className="mt-3 max-w-2xl font-sans leading-7 text-text-muted">
        Downloads will appear here after history logging is connected to the
        download route.
      </p>
    </section>
  );
}
