import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function PlaylistPage() {
  return (
    <PlaceholderPage
      description="Playlist creation is reserved for signed-in users and will build on the passage player flow."
      title="Playlists"
    />
  );
}
