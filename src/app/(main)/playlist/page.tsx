export default function PlaylistPage() {
  return (
    <section className="mx-auto w-full max-w-5xl px-6 py-12">
      <h1 className="font-display text-4xl text-text-primary">Playlists</h1>
      <p className="mt-3 max-w-2xl font-sans leading-7 text-text-muted">
        Playlist creation is reserved for signed-in users and will build on the
        passage player flow.
      </p>
    </section>
  );
}
