import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function FavouritesPage() {
  return (
    <PlaceholderPage
      description="Saved passages will appear here once the favourite action is connected to the passage player."
      title="Favourites"
    />
  );
}
