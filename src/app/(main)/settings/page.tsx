import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      description="Account preferences and default translation settings will live here."
      title="Settings"
    />
  );
}
